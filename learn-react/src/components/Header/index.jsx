import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'

Header.propTypes = {
  
};

function Header(props) {
  return (
    <div>
      
    </div>
  );
}

export default Header;